{
  "name": "timetodelay",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build . --target=browser --outfile=index.js --minify"
  },
  "dependencies": {
    "arcsecond": "^5.0.0"
  }
}
